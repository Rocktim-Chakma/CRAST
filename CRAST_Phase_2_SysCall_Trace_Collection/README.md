# Phase 2: SysCall Trace Collection

This phase contains the tracing runners and tracing protocol used to collect install-time SysCall traces. Worker-specific scripts used local paths; configurable versions are included for reproducible controlled tracing.

Do not run tracing scripts on personal or production machines. See `SECURITY.md`.
