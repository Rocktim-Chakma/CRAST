# Phase 3: Feature Extraction and Dataset Building

This phase contains registry/class-specific feature extraction scripts and reusable dataset-building utilities. The same SysCall feature extraction logic is used across NPM/PyPI and benign/malicious samples with registry/class-specific input and output paths.
